# Test Harness (placeholder)

This directory will eventually hold curl scripts and regression tests that exercise the ESPHome REST endpoints (buttons, numbers) plus OTA/safe-mode scenarios. Keeping it top-level keeps testing visible in the workflow.
